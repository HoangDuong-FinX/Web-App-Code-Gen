# Branch Appointment

Generated by the WEB/APP Factory pipeline. Module Federation remote (track C):
the Host loads `branch_appointment/App` from `dist/remoteEntry.js`, same window, no
iframe.

```bash
npm install
npm run typecheck
npm run build      # emits dist/remoteEntry.js
```

Dependency versions are pinned exactly. This scaffold was installed and built
before being committed; generated screens are added on top of it.
